name: notebook_app
description: "A Cloud Document Notebook application for Module-06."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State management layer used throughout the architecture
  provider: ^6.1.2
  
  # Fluent icon utilities for user interface controls
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true